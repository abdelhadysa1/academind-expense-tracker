# academind-expense-tracker

An expense tracker web application created with React.

## Getting started

```sh
npm i # install the package
npm start # start the dev server
npm build # build for production
```
